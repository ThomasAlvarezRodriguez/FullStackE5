<!-- Dans quete_detail.html -->
{% extends 'base.html' %}

{% block title %}{{ quete.nom }}{% endblock %}

{% block content %}
    <div class="breadcrumb">
        <a href="{% url 'home' %}">Accueil</a> > <a href="{% url 'jeux_list' %}">Liste des jeux</a> > <a href="{% url 'game_detail' jeu_id=quete.jeu.id %}">{{ quete.jeu.nom }}</a> > {{ quete.nom }}
    </div>
    <div class="content">
        <h1>{{ quete.nom }}</h1>
        <p>{{ quete.description }}</p>
        <!-- Affichez l'image si elle existe -->
        {% if quete.image %}
            <img src="{{ quete.image.url }}" alt="{{ quete.nom }}">
        {% endif %}
    </div>
{% endblock %}
